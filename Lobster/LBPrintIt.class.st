"
Supports interpretation of a DoIt + print command.

Looks the same as DoIt on the server side but the frontend will handle differently and it uses the class name
"
Class {
	#name : #LBPrintIt,
	#superclass : #LBDoIt,
	#category : #'Lobster-Protocol'
}
